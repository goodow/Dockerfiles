apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: iot-policy-svr
spec:
  selector:
    matchLabels:
      app: iot-policy-svr
  replicas: 1
  template:
    metadata:
      labels:
        app: iot-policy-svr
    spec:
      containers:
      - name: iot-policy-svr
        image: goodow/golang-development:latest
        ports:
        - containerPort: 10004
        - containerPort: 22
        volumeMounts:
        - mountPath: /go
          name: golang-src
      volumes:
       - name: golang-src
         hostPath:
           path: /home/ubuntu/gosrc/policy


---
apiVersion: v1
kind: Service
metadata:
  name: iot-policy-svr
spec:
  selector:
    app: iot-policy-svr
  type: LoadBalancer
  ports:
  - port: 36002
    targetPort: 22
    name: ssh
