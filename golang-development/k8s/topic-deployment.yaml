apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: iot-topic-svr
spec:
  selector:
    matchLabels:
      app: iot-topic-svr
  replicas: 1
  template:
    metadata:
      labels:
        app: iot-topic-svr
    spec:
      containers:
      - name: iot-topic-svr
        image: goodow/golang-development:latest
        ports:
        - containerPort: 10005
        - containerPort: 22
        volumeMounts:
        - mountPath: /go
          name: golang-src
      volumes:
       - name: golang-src
         hostPath:
           path: /home/ubuntu/gosrc/topic


---
apiVersion: v1
kind: Service
metadata:
  name: iot-topic-svr
spec:
  selector:
    app: iot-topic-svr
  type: LoadBalancer
  ports:
  - port: 36002
    targetPort: 22
    name: ssh
