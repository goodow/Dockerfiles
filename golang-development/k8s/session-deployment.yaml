apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: iot-session-svr
spec:
  selector:
    matchLabels:
      app: iot-session-svr
  replicas: 1
  template:
    metadata:
      labels:
        app: iot-session-svr
    spec:
      containers:
      - name: iot-session-svr
        image: goodow/golang-development:latest
        ports:
        - containerPort: 10003
        - containerPort: 22
        volumeMounts:
        - mountPath: /go
          name: golang-src
      volumes:
       - name: golang-src
         hostPath:
           path: /home/ubuntu/gosrc/session


---
apiVersion: v1
kind: Service
metadata:
  name: iot-session-svr
spec:
  selector:
    app: iot-session-svr
  type: LoadBalancer
  ports:
  - port: 36002
    targetPort: 22
    name: ssh
